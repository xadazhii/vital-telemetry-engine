version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: cardio_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cardio_db
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d cardio_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: cardio_redis
    ports:
      - "6379:6379"

  api:
    build: .
    container_name: cardio_api
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:password@db:5432/cardio_db
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - .:/app

  worker:
    build: .
    container_name: cardio_worker
    command: celery -A app.workers.celery_app worker --loglevel=info
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:password@db:5432/cardio_db
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - .:/app
